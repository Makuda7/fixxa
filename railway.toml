[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd client && npm install && CI=false npm run build && cd .."

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
