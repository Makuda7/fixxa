# Development environment variables for React app
# API requests will use the same origin (localhost:3000) since backend serves the React build
# No REACT_APP_API_URL needed - API calls use relative paths
