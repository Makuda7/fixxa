# Development environment variables for React app
# The proxy in package.json will forward API requests to this backend
# In development: React runs on port 3001, Express backend on port 3000

# API URL (leave empty to use proxy from package.json)
REACT_APP_API_URL=

# Port for React dev server (default is 3000, but backend uses 3000)
PORT=3001
